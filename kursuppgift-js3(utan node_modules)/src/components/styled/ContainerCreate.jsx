import React from 'react'
import styled from 'styled-components'

const StyledContainerCreate = styled.div`
    text-align: center;
    margin-bottom: 10px;
`

export default function ContainerCreate(props) {
    return (
        <div>
            <StyledContainerCreate col={props.col}>
                {props.children}
            </StyledContainerCreate>
        </div>
    )
}
