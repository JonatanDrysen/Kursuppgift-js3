import React from 'react'
import styled from 'styled-components'

const StyledContainerList = styled.div`
    padding-left: 20px;
`

export default function ContainerList(props) {
    return (
        <div>
            <StyledContainerList col={props.col}>
                {props.children}
            </StyledContainerList>
        </div>
    )
}
