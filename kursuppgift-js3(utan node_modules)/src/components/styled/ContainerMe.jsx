import React from 'react'
import styled from 'styled-components'

const StyledContainerMe = styled.div`
    width: ${props => props.col*(100/12)}%;
    height: auto;
    border: 1px solid gray;
    border-radius: 7px;
    margin-left: 30px;
    padding-left: 10px;
    padding-top: 8px;
`
export default function ContainerMe(props) {
    return (
        <StyledContainerMe col={props.col}>
            {props.children}
        </StyledContainerMe>
    )
}